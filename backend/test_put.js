const fetch = require('node-fetch');

async function testUpdate() {
  const token = 'test'; // Not actually used by adminMiddleware if we mock it, wait we can't bypass adminMiddleware from a script unless we have a token.
}
